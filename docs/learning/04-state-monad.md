# 04: State Monad - Pure Mutation Illusion\n\n## Simple Analogy\nStateT s m a = s → (a, s)  — Thread state implicitly.\nBurrito: State=ingredients, get/modify=wrap/unwrap.\n\n## PS Syntax\n```purescript\ntype Game = { pinch :: Boolean }\nupdate :: Input → StateT Game Effect Unit\nupdate inp = do\n  st ← get\n  when (dist inp < 0.02) $ modify _ { pinch = true }\n```\n\n## Manas Upgrade\nSplit tick: input → StateT update → render.\n\n## Exercise\nWrite `counter :: StateT Int Effect Unit` (inc twice → 2).