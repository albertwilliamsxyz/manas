# 05: Category Theory - Compose Everything\n\n## No Jargon First\nCat: Dots (objects) + Arrows (morphisms f: A→B). Laws: id, assoc compose (>>>).\nFunctor: map : (a→b) → (F a → F b)\nMonoid: empty + append (e.g., Mat4 id/mult)\n\n## PS\n```purescript\nnewtype Mat4 = ...\ninstance Semigroup Mat4 where append = mult\ntransform = scale >>> rotate >>> translate\n```\n\n## Manas\nScene graph: Functor Node (map Gesture). Transforms monoid.\n\n## Exercise\nFunctor law: map id = id. Test Array.