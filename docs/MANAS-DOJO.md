# Manas Dojo: Learning Pure Math Through PureScript\n\n## Welcome, Eternal Learner\n\nYou're building Manas: Immersive AR/VR for thought experiments. Hands track, gestures draw/manipulate 3D worlds. PureScript (PS) as dojo—apply lambda calculus, type theory, combinators, categories practically.\n\n**Open Mindset**: We'll start basic (what's a lambda?), build to code, spot ignorance together. Questions at end.\n\n## 1. Foundations: Math of Computation (No PhD Needed)\n\n### Lambda Calculus: Functions = Everything\n- **Core Idea**: Computation is function application. No loops/vars—just β-reduction (substitute args).\n- PS Example:\n  ```purescript\n  addOne = \\x -&gt; x + 1  -- Lambda: arg → body\n  addOne 5 == 6         -- Apply: β-red to 6\n  ```\n- In Manas: RAF tick = λ frame → (read → update → render)\n\n### Type Theory: Types as Proofs\n- **Idea**: Type says \"this data satisfies proposition\". `Maybe a` proves \"value or absent\".\n- PS: Compiler checks proofs free.\n- Manas: `Either String Unit` for init—Left=error proof.\n\n### Combinators: No Names, Pure Flow\n- **Idea**: Functions without vars (SKI combinators). Compose data transforms.\n- Manas: Joint map: λ joints → positions (fold/map).\n\n### Categories: Ultimate Glue\n- **Basics**: Objects (data), Morphisms (funcs preserving structure). Compose: f >>> g.\n- Functor: map over structures. Monoid: combine (e.g., Mat4 mult).\n- Manas Goal: Scene = category (entities → transforms).\n\n## 2. Current Code: Walkthrough (Line-by-Line Feel)\n\n### Setup (Main.purs: ROP Fortress)\n- `runExceptT`: Chain failsafes (canvas? shader compile? XR support?).\n- Why? Linear \"happy path\", explicit errors.\n\n### Hands (25 Joints × 3D)\n- VBOs dynamic: Update verts from poses.\n- Skeleton: Indices draw lines (wrist→tip).\n\n### Loop (tick: The Heart)\n1. Input: Get hands/joints/poses.\n2. Update: Copy pos → buffer (pinch dist calc).\n3. Render: Per-view (proj/view/model), draw points/lines/cube.\n\n### FFI (WebXR.purs): Safe Bridge\n- Nullables/Promises → Maybe/Aff. Opaque types hide JS mess.\n\n## 3. Patterns: What Works, What Grows\n\n| Pattern | Status | Learner Tip |\n|---------|--------|-------------|\n| ROP (ExceptT) | Strong | Linear error-handling &gt; nests. |\n| Explicit Effects | Pragmatic | `do Effect` = imperative OK for GPU. |\n| Missing: StateT | Add Soon | Pure updates: `s → s'`. |\n| Magic Nums | Fix | Consts: `cubeSize :: Number = 0.1` |\n| God Loop | Split | input/update/render pure funcs. |\n\n## 4. Refinements: Step-by-Step Dojo\n\n1. **Gesture ADT**:\n   ```purescript\n   data Gesture = Pinch | Open deriving Show\n   classify :: Hands → Gesture\n   ```\n2. **StateT**:\n   ```purescript\n   type State = { pinchActive :: Boolean }\n   ```\n3. **Linear Alg**: Add `purescript-linear-algebra`, `Mat4.mult`.\n4. **Cats**: `profunctor-lenses` for zoom (hand.tip.pos).\n\n## 5. Exercises: Dissipate Ignorance\n\n1. Eval `(λx.x x) (λx.x x)` by hand—what reduces to?\n2. Why `Maybe` functor? Write `mapMaybe`.\n3. Refactor pinch: `dist :: Vec3 → Vec3 → Number`, threshold monoid?\n4. Scene graph: `data Node = Leaf Entity | Branch (Array Node) Transform`\n\nBuild: `spago build && npm run serve:https`\n\nIgnorance is fuel—what's unclear? Next exercise/code tweak?