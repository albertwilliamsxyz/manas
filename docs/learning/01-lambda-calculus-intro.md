# 01: Lambda Calculus - The Soul of PureScript\n\n## Eternal Learner: Start Here\nLambda calc (1930s Church) proves computation = functions applying. No vars/loops—just abstraction (λ) + application. PS compiles to JS via lambdas (beta-reduction: sub args).\n\n## Basics\n- **Term**: Var (x), Abs (λx.M), App (M N)\n- **Free-Red**: (λx.x y) → y (substitute)\n\nEx: Identity = λx.x\nBool True = λt.λf.t\n\n## PS Taste\n```purescript\nid = \\x -> x\ntrue = \\t -> \\f -> t\nifThenElse = true id (const unit)  -- Church if\n```\n\n## Manas Tie\nRAF tick = λframe.(input >>> update >>> render) — pure reduction per frame.\n\n## Exercise (Dissipate Ignorance)\nReduce: (λx.xx)(λx.xx) → ? (Hints: diverges! Omega combinator—PS stack overflow analog).\nNext: Church + (λm.λn.λf.m(f n f)).